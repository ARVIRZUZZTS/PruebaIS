version: '3.9'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    entrypoint: ["/bin/sh", "-c", "sed -i 's/\\r//' .env 2>/dev/null; npm run dev"]